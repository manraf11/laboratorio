{
  "name": "laboratorio-clinico",
  "version": "2.0.0",
  "type": "module",
  "description": "Sistema de gestión para laboratorio clínico",
  "scripts": {
    "build": "tsc",
    "vercel-build": "echo 'No TypeScript compilation needed' || npm run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.111.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}